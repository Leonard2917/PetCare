<Page x:Class="PetCare.Views.MyPetsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="Animalele Mele">

    <Grid Background="#F5F7FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Animalele Mele" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Gestionează profilele și vezi istoricul pentru fiecare prieten necuvântător." 
                           Foreground="Gray" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Adaugă Animal" Background="#FF9800" Foreground="White" 
                    Padding="20,0" Height="45" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel>

                <Border Width="300" Height="380" Background="White" CornerRadius="15" Margin="0,0,30,30">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.1" ShadowDepth="5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#E3F2FD" CornerRadius="15,15,0,0">
                            <TextBlock Text="🐶" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Row="1" Margin="20">
                            <Grid>
                                <TextBlock Text="Rex" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                                <TextBlock Text="Câine" HorizontalAlignment="Right" Foreground="Gray" VerticalAlignment="Center"/>
                            </Grid>

                            <TextBlock Text="Golden Retriever • 4 Ani" Foreground="#546E7A" Margin="0,5,0,15"/>

                            <Separator Background="#ECEFF1" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Următoarea vizită:" FontWeight="Bold" FontSize="12"/>
                                <TextBlock Text="14 Ian" Foreground="#2196F3" FontWeight="Bold" Margin="5,0,0,0"/>
                            </StackPanel>

                            <Button Content="Vezi Profil Complet" Background="Transparent" Foreground="#2196F3" 
                                    BorderBrush="#2196F3" BorderThickness="1" Height="35" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Width="300" Height="380" Background="White" CornerRadius="15" Margin="0,0,30,30">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.1" ShadowDepth="5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#FCE4EC" CornerRadius="15,15,0,0">
                            <TextBlock Text="🐱" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Row="1" Margin="20">
                            <Grid>
                                <TextBlock Text="Luna" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                                <TextBlock Text="Pisică" HorizontalAlignment="Right" Foreground="Gray" VerticalAlignment="Center"/>
                            </Grid>

                            <TextBlock Text="Europeană • 2 Ani" Foreground="#546E7A" Margin="0,5,0,15"/>

                            <Separator Background="#ECEFF1" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Stare:" FontWeight="Bold" FontSize="12"/>
                                <TextBlock Text="Sănătoasă" Foreground="#4CAF50" FontWeight="Bold" Margin="5,0,0,0"/>
                            </StackPanel>

                            <Button Content="Vezi Profil Complet" Background="Transparent" Foreground="#2196F3" 
                                    BorderBrush="#2196F3" BorderThickness="1" Height="35" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Page>