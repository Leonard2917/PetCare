<Page x:Class="PetCare.Views.AdminStatsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="Statistici">

    <Grid Background="#F5F7FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="10" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Alege Clinica" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox Width="200" SelectedIndex="0">
                        <ComboBoxItem Content="Toate Clinicile"/>
                        <ComboBoxItem Content="VetLife Center"/>
                        <ComboBoxItem Content="Animal Happy"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Alege Medicul" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox Width="200" SelectedIndex="0">
                        <ComboBoxItem Content="Toți Medicii"/>
                        <ComboBoxItem Content="Dr. Popescu"/>
                        <ComboBoxItem Content="Dr. Ionescu"/>
                    </ComboBox>
                </StackPanel>

                <Button Content="Reîncarcă Datele" Background="#2196F3" Foreground="White" 
                        Height="30" Width="120" VerticalAlignment="Bottom" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#4CAF50" CornerRadius="10" Height="100" Margin="0,0,10,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Încasări" Foreground="White" FontSize="14" Opacity="0.8"/>
                    <TextBlock Text="45.200 RON" Foreground="White" FontSize="28" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#FF9800" CornerRadius="10" Height="100" Margin="5,0,5,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Programări Luna Asta" Foreground="White" FontSize="14" Opacity="0.8"/>
                    <TextBlock Text="128 Vizite" Foreground="White" FontSize="28" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#9C27B0" CornerRadius="10" Height="100" Margin="10,0,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Stocuri Critice" Foreground="White" FontSize="14" Opacity="0.8"/>
                    <TextBlock Text="3 Produse" Foreground="White" FontSize="28" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Top Servicii Solicitate" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                <StackPanel Grid.Row="1">

                    <Grid Margin="0,10">
                        <TextBlock Text="Vaccinare (45%)" VerticalAlignment="Center"/>
                        <ProgressBar Value="45" Height="20" Margin="150,0,0,0" Foreground="#2196F3"/>
                    </Grid>

                    <Grid Margin="0,10">
                        <TextBlock Text="Consult General (30%)" VerticalAlignment="Center"/>
                        <ProgressBar Value="30" Height="20" Margin="150,0,0,0" Foreground="#2196F3"/>
                    </Grid>

                    <Grid Margin="0,10">
                        <TextBlock Text="Intervenții Chirurgicale (15%)" VerticalAlignment="Center"/>
                        <ProgressBar Value="15" Height="20" Margin="150,0,0,0" Foreground="#2196F3"/>
                    </Grid>

                    <Grid Margin="0,10">
                        <TextBlock Text="Deparazitare (10%)" VerticalAlignment="Center"/>
                        <ProgressBar Value="10" Height="20" Margin="150,0,0,0" Foreground="#2196F3"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>