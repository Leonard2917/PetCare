<Page x:Class="PetCare.Views.AdminUsersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Gestionare Utilizatori">

    <Grid Background="#F5F7FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Baza de Date Utilizatori" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <TabControl Grid.Row="1" BorderThickness="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="👨‍⚕️ Medici Activi">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="20">
                        <TextBlock Text="Caută Medic:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="200" Padding="5"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Width="50"/>
                            <DataGridTextColumn Header="Nume Complet" Width="200"/>
                            <DataGridTextColumn Header="Clinică" Width="150"/>
                            <DataGridTextColumn Header="Email" Width="200"/>
                            <DataGridTemplateColumn Header="Acțiuni" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🗑️ Elimină Cont" Background="Red" Foreground="White" BorderThickness="0" Height="25" Width="100"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="👤 Clienți / Proprietari">
                <Grid Background="White" Margin="0,10,0,0">
                    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Width="50"/>
                            <DataGridTextColumn Header="Nume Proprietar" Width="200"/>
                            <DataGridTextColumn Header="Nr. Animale" Width="100"/>
                            <DataGridTextColumn Header="Telefon" Width="150"/>
                            <DataGridTemplateColumn Header="Acțiuni" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🗑️ Elimină Cont" Background="Red" Foreground="White" BorderThickness="0" Height="25" Width="100"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>