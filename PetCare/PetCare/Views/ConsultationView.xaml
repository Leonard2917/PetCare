<Page x:Class="PetCare.Views.ConsultationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="Fisa Consultatie">

    <Grid Background="#F5F7FA" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" Margin="0,0,20,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.05"/>
                    </Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Detalii Pacient" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="#2C3E50"/>

                            <Button Grid.Column="1" Content="📂 Vezi Dosar / Istoric" Background="#E3F2FD" Foreground="#1976D2" 
                                    Padding="15,8" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                                    ToolTip="Deschide fișa completă (Vaccinuri, Istoric boli)">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Nume Animal" Foreground="Gray" FontSize="12"/>
                                <TextBox Text="Rex" IsReadOnly="True" Background="#F5F7FA" Padding="10,5" BorderThickness="0" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="10,0,10,0">
                                <TextBlock Text="Specie / Rasă" Foreground="Gray" FontSize="12"/>
                                <TextBox Text="Câine / Labrador" IsReadOnly="True" Background="#F5F7FA" Padding="10,5" BorderThickness="0" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                                <TextBlock Text="Vârstă / Greutate" Foreground="Gray" FontSize="12"/>
                                <TextBox Text="4 ani / 28kg" IsReadOnly="True" Background="#F5F7FA" Padding="10,5" BorderThickness="0" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.05"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Raport Medical" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50"/>

                        <TextBlock Text="Simptome / Motivul vizitei" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#546E7A"/>
                        <TextBox Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10" BorderBrush="#CFD8DC"/>

                        <TextBlock Text="Diagnostic" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#546E7A"/>
                        <TextBox Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10" BorderBrush="#CFD8DC"/>

                        <TextBlock Text="Tratament Aplicat / Recomandări" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#546E7A"/>
                        <TextBox Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10" BorderBrush="#CFD8DC"/>

                        <CheckBox Content="Necesită revenire (Follow-up)" Margin="0,20,0,0" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Column="1">

            <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Materiale &amp; Servicii" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50"/>

                    <TextBlock Text="Selectează Serviciul (Manopera):" Margin="0,5" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cmbServicii" Height="40" Padding="10,0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Consultatie Generala (150 RON)"/>
                        <ComboBoxItem Content="Vaccinare (50 RON + Vaccin)"/>
                        <ComboBoxItem Content="Deparazitare"/>
                        <ComboBoxItem Content="Tuns"/>
                        <ComboBoxItem Content="Ecografie"/>
                    </ComboBox>

                    <TextBlock Text="Materiale Consumate (din Stoc):" Margin="0,20,0,5" FontWeight="SemiBold"/>
                    <TextBlock Text="Selectarea scade automat stocul!" FontSize="11" Foreground="#FF9800" Margin="0,0,0,5"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" Text="Alege produs..." IsEditable="True" Height="40" Margin="0,0,10,0" Padding="10,0" VerticalContentAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="1 buc" Height="40" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontWeight="Bold"/>
                    </Grid>

                    <Button Content="+ Adaugă la Bon" Margin="0,15,0,0" Background="#FF9800" Foreground="White" Height="40" BorderThickness="0" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <Border Background="#2C3E50" CornerRadius="10" Padding="25">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="TOTAL DE PLATĂ" Foreground="White" HorizontalAlignment="Center" Opacity="0.7" FontSize="14"/>
                    <TextBlock Text="235 RON" Foreground="White" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,25"/>

                    <StackPanel Margin="0,0,0,20">
                        <Grid Margin="0,2">
                            <TextBlock Text="• Consult General" Foreground="#B0BEC5"/>
                            <TextBlock Text="150 RON" Foreground="White" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="• Vaccin Antirabic" Foreground="#B0BEC5"/>
                            <TextBlock Text="80 RON" Foreground="White" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="• Seringă 5ml" Foreground="#B0BEC5"/>
                            <TextBlock Text="5 RON" Foreground="White" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>

                    <Separator Background="#546E7A" Margin="0,0,0,20"/>

                    <Button Content="💾 Salvează și Finalizează" Background="#4CAF50" Foreground="White" 
                            Height="50" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="25"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="🖨️ Printează Rețetă" Background="Transparent" Foreground="#90A4AE" 
                             Margin="0,10,0,0" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>