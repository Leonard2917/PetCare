<Page x:Class="PetCare.Views.AdminApprovalsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Aprobări Conturi">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cereri de Înregistrare (Medici)" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Acești utilizatori așteaptă validarea manuală pentru a accesa sistemul." Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" 
                  BorderBrush="#E0E0E0" RowHeight="60" HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nume Medic" Width="200" Binding="{Binding Nume}"/>
                <DataGridTextColumn Header="Specializare" Width="150" Binding="{Binding Specializare}"/>
                <DataGridTextColumn Header="Cod Parafă / Licență" Width="150" Binding="{Binding CodLicenta}"/>
                <DataGridTextColumn Header="Data Cererii" Width="120" Binding="{Binding Data}"/>

                <DataGridTemplateColumn Header="Decizie" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✔ Acceptă" Background="#4CAF50" Foreground="White" Width="90" Height="35" Margin="0,0,10,0" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Content="❌ Respinge" Background="#F44336" Foreground="White" Width="90" Height="35" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>