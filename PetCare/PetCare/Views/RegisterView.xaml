<Page x:Class="PetCare.Views.RegisterView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:PetCare.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="Register PetCare">

    <Page.DataContext>
        <vm:RegisterViewModel />
    </Page.DataContext>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E3F2FD" Offset="0.0"/>
                <GradientStop Color="#BBDEFB" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="White" Width="450" MaxHeight="700" VerticalAlignment="Center" CornerRadius="20" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="#1C3144" BlurRadius="30" ShadowDepth="10" Opacity="0.2"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly" Margin="-10,0,-10,0" Padding="10,0,10,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Creează Cont" FontSize="28" FontWeight="Bold" 
                               Foreground="#1565C0" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Text="Nume" FontWeight="SemiBold" Foreground="#455A64" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Nume, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Prenume" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Prenume, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Telefon" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Telefon, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Email" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Rol" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                    <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <StackPanel Visibility="{Binding IsClinicSelectionVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Clinica" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                        <ComboBox ItemsSource="{Binding Clinics}" SelectedItem="{Binding SelectedClinic}" DisplayMemberPath="Nume" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsClinicCreationVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Nume Clinică Nouă" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding ClinicaNouaNume, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>
                        
                        <TextBlock Text="Adresă Clinică" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding ClinicaNouaAdresa, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>
                        
                        <TextBlock Text="Telefon Clinică" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding ClinicaNouaTelefon, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="Parolă" FontWeight="SemiBold" Foreground="#455A64" Margin="0,10,0,5"/>
                    <PasswordBox x:Name="txtPass" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Confirmă Parola" FontWeight="SemiBold" Foreground="#455A64" Margin="0,15,0,5"/>
                    <PasswordBox x:Name="txtConfirmPass" Height="35" Padding="5,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,15,0,0" TextWrapping="Wrap"/>

                    <Button Content="ÎNREGISTREAZĂ-TE" Height="45" Margin="0,20,0,0"
                            Click="RegisterButton_Click"
                            Background="#1976D2" Foreground="White" FontWeight="Bold" Cursor="Hand">
                         <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="22" BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1565C0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Content="Înapoi la Login" Margin="0,15,0,0" Click="BackToLogin_Click"
                            Background="Transparent" Foreground="#78909C" Cursor="Hand" BorderThickness="0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
